// jkcoxson
// Handles downloading and mounting an image

use rusty_libimobiledevice::libimobiledevice::Device;

pub fn mount(device: &Device) -> Result<(), ()> {
    // Get the device's version
    todo!()
}
